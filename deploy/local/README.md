# Local deployment

The Mac collector source is in `../../agent`. Build the two local images from
that directory, then deploy `agent/compose.yaml` through Portainer with the
variables from the private `agent/config.env` file.
